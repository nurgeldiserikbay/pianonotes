<script lang="ts" setup>
/**
 * Значок входа в раздел «Другие игры».
 *
 * Сетка из четырёх плиток — привычная метафора «ещё приложения». Контур, а не
 * заливка, и цвет из `currentColor`: значок должен вести себя как соседние
 * кнопки звука и настроек, а не выделяться на их фоне. Насыщенный цвет здесь
 * ничего не кодирует, поэтому его и нет.
 *
 * Прямоугольники намеренно чуть разные — по радиусу и на доли пикселя по
 * месту. Идеальная сетка читается как системная иконка ОС; небольшая
 * неровность делает значок частью рисованного интерфейса игры.
 *
 * СГЕНЕРИРОВАНО: tools/templates/OtherGamesIcon.vue → tools/rollout-promo.mjs.
 * Правьте шаблон, а не копию: следующий запуск скрипта затрёт правку.
 */
</script>

<template>
	<svg
		viewBox="0 0 24 24"
		fill="none"
		stroke="currentColor"
		stroke-width="1.9"
		stroke-linejoin="round"
		aria-hidden="true"
		focusable="false"
	>
		<rect x="3.4" y="3.6" width="7.2" height="7.0" rx="2.2" />
		<rect x="13.5" y="3.3" width="7.1" height="7.3" rx="2.7" />
		<rect x="3.2" y="13.5" width="7.3" height="7.1" rx="2.6" />
		<rect x="13.4" y="13.4" width="7.2" height="7.2" rx="2.1" />
	</svg>
</template>
